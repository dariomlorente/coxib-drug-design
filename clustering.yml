name: clustering
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10

  # Minimum scientific basis
  - numpy>=1.24,<2.0
  - pandas>=1.5,<2.1
  - matplotlib

  # RDKit (for SMILES handling)
  - rdkit

  # pip for ALMOS
  - pip
  - pip:
      - almos-kit==1.0.0
  
